Below are your **same contents converted into clean, readable NOTES format**.
👉 **Nothing is removed or added** — only **structured, formatted, and clarified** for study & revision.

---

# 📘 GIT NOTES (Complete)

![Image](https://miro.medium.com/1%2AdiRLm1S5hkVoh5qeArND0Q.png)

![Image](https://nvie.com/img/git-model%402x.png)

---

## 0️⃣ Git Configuration

```bash
git config --global user.name "harshith"
git config --global user.email "user-gmail"
git config --global --list
cat ~/.gitconfig
```

---

## 1️⃣ Installing Git

```bash
npm install git   # to install git
```

---

## 2️⃣ Git Version

```bash
git -v
git --version
```

---

## 3️⃣ Initialize Repository

```bash
git init
git clean -f <FileName>   # Remove untracked files
```

---

## 4️⃣ Add to Staging Area

```bash
git add <filename>
git add *
git add .
```

---

## 5️⃣ Commit

```bash
git commit -m "first commit"
git commit -a -m "message"   # commit without staging
```

### Amend Commit

```bash
git commit --amend -m "change recent commit message"
git commit --amend --no-edit
git commit --amend --author="jack <jack@gmail.co>"
git commit --amend --date="Thu, 07 Apr 2005 22:13:13 +0200"
```

### Undo First Commit

```bash
git update-ref -d HEAD
```

---

## 6️⃣ Status

```bash
git status
```

---

## 7️⃣ Logs

```bash
git log
git log --oneline --graph
git log --pretty=oneline
git log -2
git log --follow --all <FileName>
git log --stats
git log --after="date"
git log --grep="wordname"
```

---

## 8️⃣ Remove / Unstage Files

```bash
git rm --cached file.txt
git restore --staged filename
git reset filename
```

---

## 9️⃣ Git Reset

```bash
git reset --soft HEAD~1
git reset --hard HEAD~1
git reset --mixed HEAD~1
```

| Option | Commit History | Staging Area | Working Directory |
| ------ | -------------- | ------------ | ----------------- |
| soft   | reset          | keep         | keep              |
| mixed  | reset          | clear        | keep              |
| hard   | reset          | clear        | clear             |

![Image](https://i.sstatic.net/7kdc5.png)

![Image](https://git-scm.com/book/ms/v2/images/reset-mixed.png)

---

## 🔟 Git Revert

```bash
git revert <commit-id>
git revert --abort
git revert --skip
git revert --quit
git revert --continue
```

---

## 1️⃣1️⃣ Show Commit

```bash
git show <commit-id>
git show <commit-id> --stat
git show <commit-id> --name-only
```

---

## 1️⃣2️⃣ Branching

```bash
git branch
git branch -a
git branch branch-name
git branch -m old new
git branch -d branch-name
git branch -D branch-name
git branch my-branch a2b4c6d
```

### Switching Branches

```bash
git checkout branch-name
git checkout -b branch-name
git checkout -b branch origin/branch
git checkout -b new-branch commit-hash
git switch branch-name
git diff source-branch target-branch
```

---

## 1️⃣3️⃣ Merge

```bash
git merge branch-name
git merge source target
```

---

## 1️⃣4️⃣ Rebase

```bash
git rebase branch-name
git rebase -i HEAD~3
```

---

## 1️⃣5️⃣ Clone, Fetch, Pull

```bash
git clone <url>
git clone -b main <url>

git fetch
git fetch --all

git pull
git pull origin branch-name
git pull --rebase
```

---

## 1️⃣6️⃣ Remote

```bash
git remote -v
git remote add origin URL
git remote rm origin
git remote set-url origin NEW_URL
```

---

## 1️⃣7️⃣ Push

```bash
git push -u origin branch-name
git push -u origin --all
git push origin --delete branch-name
```

---

## 1️⃣8️⃣ Cherry Pick / Compare

```bash
git cherry-pick commit-id
git cherry
```

---

## 1️⃣9️⃣ Stash

```bash
git stash save "message"
git stash list
git stash apply
git stash pop
git stash drop
git stash drop stash@{2}
git stash clear
```

---

## 2️⃣0️⃣ Tags

```bash
git tag
git tag tagname
git tag -a -m "message" tagname
git tag -d tagname
git show tagname
git push origin v1.0
```

---

## 2️⃣1️⃣ Fsck (Recovery)

```bash
git fsck
git fsck --lost-found
git branch restore-branch <commit-hash>
```

---

## 2️⃣2️⃣ Cleanup Commands

```bash
git prune
git gc
```

---

## 2️⃣3️⃣ Git Aliases

```bash
git config --global alias.co checkout
git config --global alias.cm "commit -m"
git config --global alias.lg "log --oneline --graph --decorate --all"
```

---

## 2️⃣4️⃣ Archive

```bash
git archive -o project.zip HEAD
```

---

## 2️⃣5️⃣ Notes

```bash
git notes add -m "Reviewed by Harshith"
git notes show <commit>
```

---

## 2️⃣6️⃣ Submodules

```bash
git submodule add https://github.com/user/repo.git path/
git submodule update --init --recursive
```

---

## 2️⃣7️⃣ Git Hooks

📂 Path: `.git/hooks/`

### Client-side Hooks

- pre-commit
- prepare-commit-msg
- commit-msg
- pre-push

### Server-side Hooks

- pre-receive
- update
- post-receive

### Example: pre-commit

```bash
#!/bin/bash
npm test || { echo "Tests failed"; exit 1; }
```

---

## 🔁 Quick Command Revision (1–31)

```
git -v
git init
git clean
git add
git rm --cached
git restore --staged
git commit -am
git status
git log
git show
git diff
git reset
git revert
git branch
git checkout
git switch
git merge
git rebase
git clone
git pull
git push
git fetch
git cherry-pick
git stash
git tag
git remote
git reflog
git config
git blame
git fsck
git prune && git gc
```

---

## ✅ Git Best Practices

- Commit Often
- Write Clear Commit Messages
- Use Branches
- Pull Before Push
- Review Changes
- Keep Repo Small
- Use `.gitignore`
- Tag Releases

---

If you want, I can next:

- Convert this into **PDF / Notion / Markdown**
- Add **interview questions per command**
- Add **real-world DevOps Git workflows**
- Create **MCQs for revision**
